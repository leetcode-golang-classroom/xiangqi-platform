[GLOBAL]
pants_version = "2.27.0"
backend_packages = [
  "pants.backend.experimental.go",
  "pants.backend.shell",
]

[golang]
# 容許使用系統已安裝的 Go（gvm 管理的 go1.25.0）
minimum_expected_version = "1.21"

[go-test]
# 預設帶 -v 方便看測試輸出
args = ["-v"]
